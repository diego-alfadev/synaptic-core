---
description: "{{ONE_LINE_DESCRIPTION}}"
updated: "{{YYYY-MM-DD}}"
status: active  # active | stale | archived
type:           # overview | lesson | reference
# validated: "{{YYYY-MM-DD}}"  # uncomment when a human has verified this page is still accurate
---

# {{TITLE}}

<!-- STYLE NOTES
- Dense overview style is welcome — this is a wiki page, not a bullet list.
- Soft budget: ~150 lines. If the page grows beyond that, split into linked sub-pages and
  replace the excess with `[[subpage-name]]` links.
- Link related pages with `[[wikilinks]]` written at capture time (not retroactively).
- Naming: unique kebab-case filename across knowledge/ (no duplicates, no spaces).
- Point-in-time facts (IDs, endpoints, env data, credentials hints) live HERE in the page
  about that system, tagged with the updated: date above. There is no separate inventory.
- When a fact changes, update the page and bump updated:.
-->

<!-- Write content below this line -->
